import React from 'react';

const NotFound = (props) => {
	return (
		<div className="header not-found">
		<h3>Sorry, that page can't be found</h3>
		<h5>Return to the home page and create a new library</h5>
		</div>
		)
}

export default NotFound;